# Standalone Auto-EDA & Report Builder — 필수 패키지
# 로컬 설치:  pip install -r requirements.txt
#
# 상한(<)은 예고 없는 메이저 업데이트로 동작이 바뀌는 것을 막기 위한 것입니다.
# Streamlit Community Cloud 도 이 파일을 그대로 사용합니다.

# GUI
streamlit>=1.41,<2

# 데이터 처리
pandas>=2.0,<4
numpy>=1.24,<3
openpyxl>=3.1,<4          # .xlsx 읽기
xlrd>=2.0,<3              # 구형 .xls 읽기

# 모델링
scikit-learn>=1.3,<2

# 시각화
matplotlib>=3.7,<4

# 보고서
python-docx>=1.1,<2

# --- 선택 사항 -------------------------------------------------------------
# 설치되어 있으면 후보 모델에 자동 포함되고, 없어도 정상 동작합니다.
# Community Cloud 는 메모리가 넉넉하지 않으므로 기본적으로 꺼 두었습니다.
# xgboost>=2.0,<4
# lightgbm>=4.0,<5
